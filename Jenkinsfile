pipeline
{
  agent any

  tools {
    // Install the Maven version "M3" and add to the path
    maven "Maven 3"
  }

  stages {
    stage('Build') {
      steps
          // Get some code from a GitHub repository
          git "
